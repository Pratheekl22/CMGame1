class storyNode {
    id;
    text;
    options;
    foundWeapon;

    constructor(id, text, options, foundWeapon) {
        this.id = id;
        this.text = text;
        this.options = options;
        this.foundWeapon = foundWeapon;
    }
}

